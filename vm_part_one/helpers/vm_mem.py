#Stack Memory 

#execute push pop operations using virtual mem segments constant, local, arg, this, that, and temp 
def basic_test(operations): 
    return operations

# executes push/pop operations using virtual mem segments pointer, this, and that
def pointer_test(operations): 
    return operations 

# executes push/pop operations using the virtual memory segment static
def stack_test(operations): 
    return operations 